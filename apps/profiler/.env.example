# Used by server/utils/supabase.ts for profile-sharing storage.
# See server/db/well_shares.sql for the table this connects to.
#
# Values are never committed here. For local `nuxt dev`, copy this file to
# `.env` and fill them in.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
