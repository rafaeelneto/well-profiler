*/node_modules/
/node_modules
*/.env*
*/debug.log
*/build/
*.env*
node_modules

# License files must stay byte-identical to the canonical Apache-2.0 text —
# reformatting them breaks GitHub's license detection (shows "Other").
LICENCE.md
LICENSE
**/LICENCE.md
**/LICENSE

# Deprecated Next.js app — frozen for reference, not linted or formatted.
# Without this, `pnpm format` rewrites ~30 files nobody intends to change.
apps/well-profiler/

# Generated by `pnpm --filter=@welldot/core generate:schema` and checked by CI.
# Prettier collapses its arrays, the generator re-expands them, and the two
# fight — leaving the drift guard permanently red.
packages/core/docs/schema/
